ALGORITHME ExempleIndentation
constante:
    MAXIMUM <-- 100
variable:
    age: entier
    message: chaine
DEBUT
    lire (age)

    si age < 18 alors
     message <-- " mineur"
    sinon
     message <-- " majeur"
    fsi ×
    écrire ( "vous êtes ", message )
FIN